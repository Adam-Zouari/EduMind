embedding:
  indexing_device: cpu
  query_device: cpu

vector:
  persist_directory: artifacts/rag/vector_store.local

# Optional benchmark-promoted stack; immutable revision is mandatory when enabled.
retrieval:
  strategy: rrf
  reranker_revision:
  reranker_device: cpu

generation:
  model_name: qwen3:1.7b
  base_url: http://127.0.0.1:11434
